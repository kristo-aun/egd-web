server:
    port: 10344
    address: localhost

# security configuration (this key should be unique for your application, and kept secret)
app.security.rememberme.key: 388e8e186183c839451e1df7b0aceb3892fff430

async:
    corePoolSize: 2
    maxPoolSize: 50
    queueCapacity: 10000

spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/egd
        username: egdrole
        password: redherring

mail:
    host: localhost
    port: 25
    username:
    password:
    protocol: smtp
    tls: false
    auth: false

authentication:
    oauth:
        clientid: egdapp
        secret: mySecretOAuthSecret
        # Token is valid 30 minutes
        tokenValidityInSeconds: 1800
